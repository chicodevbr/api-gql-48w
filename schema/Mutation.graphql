type Mutation {
  createPost(post: PostInput): Post
  updatePost(id: String, post: PostInput): Post
  deletePost(id: String): Post
}

input PostInput {
  title: String
  content: String
  coverImage: String
  author: String
  tags: [String]
  slug: String
  createdAt: Date
}
